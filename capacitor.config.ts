import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rivitan.volleyball',
  appName: 'Volleyball Rotation Trainer',
  webDir: 'dist',
  backgroundColor: '#0f172a',
};

export default config;
